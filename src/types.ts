export type Device = {
  name: string;
  dev_num: string;
}

export type Matter = {
  name: string;
}

export type Desktop = {
  name: string;
  x: number;
  y: number;
  width: number;
  height: number;
}